name: AI Tools Affiliate Review

on:
  # Chạy thủ công
  workflow_dispatch:
  # Chạy 3 lần/ngày: 8h, 14h, 20h Vietnam (1:00, 7:00, 13:00 UTC)
  schedule:
    - cron: '0 1 * * *'    # 8 AM Vietnam
    - cron: '0 7 * * *'    # 2 PM Vietnam
    - cron: '0 13 * * *'   # 8 PM Vietnam

jobs:
  affiliate-scraper:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        pip install -r ai-daily-brief/requirements.txt

    - name: Run Affiliate Scraper
      env:
        FIRECRAWL_API: ${{ secrets.FIRECRAWL_API }}
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        cd ai-daily-brief
        python3 affiliate_scraper.py

    - name: Commit and push affiliate content
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add ai-daily-brief/affiliate/
        git diff --staged --quiet || git commit -m "Update AI Tools Affiliate $(date +%Y-%m-%d)"
        git push
